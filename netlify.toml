[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions.esbuild]
  format = "esm"

[[redirects]] 
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200